using UnityEngine;
using System.Collections;

public static class Definitions {
    public enum Mana_Colors {
        Black,
        Blue,
        Green,
        Purple,
        Red,
        White,
        Yellow
    }
}
